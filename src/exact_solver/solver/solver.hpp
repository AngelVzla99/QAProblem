#define FOR(i, n, m) for (int i = n; i < (int)m; i++)
#include <algorithm>
#include <iostream>
#include <map>
#include <string>
#include <vector>

#include "../../QAP/QAP.hpp"

using namespace std;

typedef long long ll;

QAP_solution solver(QAP problem);